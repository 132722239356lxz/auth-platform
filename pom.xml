<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.liang</groupId>
  <artifactId>auth-platform</artifactId>
  <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Archetype - auth-platform</name>
  <url>http://maven.apache.org</url>
    <modules>
        <module>auth-server</module>
        <module>system-server</module>
        <module>auth-flow</module>
        <module>auth-message</module>
        <module>ai-agent-server</module>
        <module>gateway</module>
        <module>common-core</module>
        <module>subsystem-sdk</module>
        <module>resource-server-starter</module>
        <module>knife4j-aggregation</module>
        <module>log-server</module>
    </modules>

    <properties>
        <spring.boot.version>3.2.5</spring.boot.version>
        <spring.cloud.version>2023.0.1</spring.cloud.version>
        <spring.cloud.alibaba.version>2023.0.1.2</spring.cloud.alibaba.version>
        <spring.auth.server.version>1.2.4</spring.auth.server.version>
        <nacos.client.version>2.3.2</nacos.client.version>
        <knife4j.version>4.5.0</knife4j.version>
        <springdoc.version>2.3.0</springdoc.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- ======================== 环境配置默认值(dev) ======================== -->
        <!-- 安全提示：此处仅放置非敏感配置。Nacos 密码等凭证一律不在构建期固化，
             改为在 application.yml 中以 ${NACOS_PASSWORD:xxx} 形式于运行期注入，
             避免明文密码被打进 jar 包随制品分发造成泄露。 -->
        <nacos.server-addr>127.0.0.1:8848</nacos.server-addr>
        <nacos.namespace>14a61398-a648-4477-ad14-6e7b72423d43</nacos.namespace>
        <nacos.group>AUTH_GROUP</nacos.group>
        <nacos.username>nacos</nacos.username>
        <server.host>127.0.0.1</server.host>
        <gateway.host>localhost</gateway.host>
        <gateway.port>8080</gateway.port>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- SpringBoot 父依赖 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Authorization Server -->
            <dependency>
                <groupId>org.springframework.security</groupId>
                <artifactId>spring-security-oauth2-authorization-server</artifactId>
                <version>${spring.auth.server.version}</version>
            </dependency>
            <!-- SpringCloudAlibaba -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Nacos Discovery -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
            </dependency>
            <!-- Nacos Config -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
            </dependency>
            <!-- Spring Cloud Gateway -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-gateway</artifactId>
                <version>4.1.1</version>
            </dependency>
            <!-- SpringDoc OpenAPI (统一版本) -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <!-- Knife4j 聚合 -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-aggregation-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <!-- Knife4j OpenAPI3 Jakarta Starter -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <!-- Lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>1.18.30</version>
            </dependency>
            <!-- Resilience4j 熔断限流 -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-spring-boot3</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-reactor</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>
            <!-- Redis (Gateway限流) -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
                <version>${spring.boot.version}</version>
            </dependency>
            <!-- TransmittableThreadLocal (线程池上下文透传) -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>transmittable-thread-local</artifactId>
                <version>2.14.4</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ======================== Maven 多环境 Profile ======================== -->
    <profiles>
        <!-- 开发环境(默认激活) -->
        <profile>
            <id>dev</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <nacos.server-addr>127.0.0.1:8848</nacos.server-addr>
                <nacos.namespace>14a61398-a648-4477-ad14-6e7b72423d43</nacos.namespace>
                <nacos.group>AUTH_GROUP</nacos.group>
                <nacos.username>nacos</nacos.username>
                <server.host>127.0.0.1</server.host>
                <gateway.host>localhost</gateway.host>
                <gateway.port>8080</gateway.port>
            </properties>
        </profile>
        <!-- 测试环境: mvn clean package -Ptest -->
        <profile>
            <id>test</id>
            <properties>
                <nacos.server-addr>test-nacos.example.com:8848</nacos.server-addr>
                <nacos.namespace>test-namespace-uuid-here</nacos.namespace>
                <nacos.group>AUTH_GROUP</nacos.group>
                <nacos.username>nacos</nacos.username>
                <server.host>test.example.com</server.host>
                <gateway.host>test-gateway.example.com</gateway.host>
                <gateway.port>8080</gateway.port>
            </properties>
        </profile>
        <!-- 生产环境: mvn clean package -Pprod -->
        <profile>
            <id>prod</id>
            <properties>
                <nacos.server-addr>prod-nacos.example.com:8848</nacos.server-addr>
                <nacos.namespace>prod-namespace-uuid-here</nacos.namespace>
                <nacos.group>AUTH_GROUP</nacos.group>
                <nacos.username>nacos</nacos.username>
                <server.host>prod.example.com</server.host>
                <gateway.host>gateway.example.com</gateway.host>
                <gateway.port>8080</gateway.port>
            </properties>
        </profile>
    </profiles>

    <build>
        <!-- 资源过滤: 对YAML文件启用 @...@ 占位符替换 -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>**/*.yml</include>
                    <include>**/*.yaml</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>**/*.yml</exclude>
                    <exclude>**/*.yaml</exclude>
                </excludes>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.3.1</version>
                    <configuration>
                        <!-- 只使用 @...@ 占位符, 避免与Spring的 ${...} 冲突 -->
                        <delimiters>
                            <delimiter>@</delimiter>
                        </delimiters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>17</source>
                        <target>17</target>
                        <encoding>UTF-8</encoding>
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>1.18.30</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
