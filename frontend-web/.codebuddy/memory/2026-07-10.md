## 2026-07-10 — 对接认证中心 REST API 文档

### 修改内容
根据 `/api/auth` 接口文档完成了前端对接：

1. **`src/types/index.ts`** — 新增 `LoginRequest`、`LoginResponse`、`LoginUserInfo`、`RefreshTokenRequest` 类型
2. **`src/api/auth.ts`** — 新增 `login()` 密码登录 和 `refreshTokenApi()` 刷新 Token 接口
3. **`src/utils/request.ts`** — `handleTokenExpired()` 改为调用 `/auth-server/api/auth/refresh` 自动刷新 Token，实现并发请求排队机制
4. **`src/views/login/index.vue`** — 开发模式改为调用真实 `login()` API，不再使用模拟 Token
5. **`vite.config.ts`** — `/auth-server` 代理 target 默认值从 `8080` 改为 `9000`（auth-server 端口）
